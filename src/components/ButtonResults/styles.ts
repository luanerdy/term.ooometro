import styled from "styled-components";

const ButtonResultStyles = styled.button`
  width: 80%;
  max-width: 300px;
  aspect-ratio: 5 / 1;

  margin: 20px auto;

  border-radius: 20px;
  font-size: 1.4em;

  background-color: var(--color-white);
  color: var(--color-primary);

  border: none;
  cursor: pointer;
`;

export { ButtonResultStyles };
