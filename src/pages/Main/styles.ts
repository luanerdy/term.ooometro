import styled from "styled-components";

const PageStyles = styled.main`
  display: flex;
  flex-flow: column nowrap;
  align-items: center;

  position: relative;

  width: 100%;
  max-width: 720px;
  height: 100vh;

  margin: 0 auto;
  padding: 20px;

  text-align: center;
`;

export { PageStyles };
